using System;
using System.Collections.Generic;
using System.Drawing;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace EdgeDetectionLib.Tests
{
    public class PixelMatrixTests
    {
        //public void ()
    //                using var bitmap = new Bitmap(@"bird.jpg");

    //_sut.SetBitmap(bitmap);
    //        var actual = _sut._pixelMatrix;
    //var dimension = bitmap.PixelFormat == System.Drawing.Imaging.PixelFormat.Format8bppIndexed ? 1 : 3;

    //Assert.IsType<PixelMatrix>(actual);
    //        Assert.NotNull(actual);
    //        Assert.Equal(bitmap.Height, actual.Height);
    //        Assert.Equal(bitmap.Width, actual.Width);
    //        Assert.Equal(dimension, actual.Dimensions);
    }
}
